(defun problem-2-1 (arg)
    (first (last arg)))

(defun problem-2-1 (arg)
    (car (last arg)))

(defun problem-2-2 (arg)
    (first (reverse arg)))

(defun problem-2-3 (arg)
    (car (reverse arg)))